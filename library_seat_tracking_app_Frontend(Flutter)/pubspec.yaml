name: library_seat_tracking_app
description: A new Flutter project.

publish_to: 'none'

environment:
  sdk: '^3.11.0'

dependencies:
  firebase_core: ^4.8.0
  firebase_messaging: ^16.2.1
  flutter:
    sdk: flutter

  geolocator: ^14.0.2
  http: ^1.6.0
  mobile_scanner: ^7.2.0
  shared_preferences: ^2.5.5

  path_provider: ^2.1.5
  open_filex: ^4.7.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true